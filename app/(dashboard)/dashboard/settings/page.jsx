export default function Page() {
  return (
    <div>
      <h1 className="font-headline font-bold text-2xl mb-2">Settings</h1>
      <p className="text-stone text-sm max-w-lg">Site-wide settings — reads/writes GET & PATCH /api/settings.</p>
    </div>
  );
}
