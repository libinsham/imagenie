export default function Page() {
  return (
    <div>
      <h1 className="font-headline font-bold text-2xl mb-2">Users</h1>
      <p className="text-stone text-sm max-w-lg">User management UI — reads from GET /api/users once an admin auth check is added.</p>
    </div>
  );
}
