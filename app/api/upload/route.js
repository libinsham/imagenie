import { errorResponse } from "@/lib/helpers";

/**
 * Scaffold only — see lib/cloudinary.js. Implement once you have
 * Cloudinary (or S3/UploadThing/etc) credentials configured.
 */
export async function POST() {
  return errorResponse("File upload is not implemented yet.", 501);
}
